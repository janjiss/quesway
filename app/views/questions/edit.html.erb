<% title "Edit question" %>
<ul id="right_menu">
  <li><%= link_to "Back to survey", survey_path(@survey), :class=> "back" %></li>
</ul>
<h3><%= @survey.name %></h3>

<%= render 'form' %>

<p><%=  %></p>
